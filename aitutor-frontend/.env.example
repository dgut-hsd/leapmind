# Public frontend configuration only — never add AI provider keys here.
# Copy to .env.local for local development; .env.local is Git-ignored.

# Leave empty to use Vite's /api proxy (recommended for local development).
# VITE_API_BASE=

# Keep false for real M4 API validation. Set true only for UI-only Mock demonstrations.
VITE_LECTURE_MOCK=false